# ============================================================
# AI Model-as-a-Service (MaaS) Platform — Dependencies
# ============================================================
# Pinned versions for reproducible builds. Update with caution.

# --- Core Framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.18           # File upload support

# --- Data Validation & Settings ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database (PostgreSQL + Async) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0                    # Async PostgreSQL driver
alembic==1.14.1                    # Schema migrations

# --- Redis (Cache + Queue) ---
redis[hiredis]==5.2.1              # hiredis for C-level performance

# --- ML Inference Runtimes ---
scikit-learn==1.6.1
numpy==2.2.1
onnxruntime==1.20.1
# torch — install separately per CUDA version:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu

# --- Observability ---
prometheus-client==0.21.1
structlog==24.4.0                  # Structured JSON logging

# --- Security & Utilities ---
python-jose[cryptography]==3.3.0   # JWT tokens (future auth)
passlib[bcrypt]==1.7.4             # Password hashing (future auth)
httpx==0.28.1                      # Async HTTP client for tests
aiofiles==24.1.0                   # Async file I/O

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
pytest-cov==6.0.0
